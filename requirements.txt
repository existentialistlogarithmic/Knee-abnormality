# Local environment: EDA on CSVs and DICOM *headers* only.
# Nothing here assumes a GPU. Training deps live in the Kaggle kernel images,
# which we do not try to reproduce locally (see docs/STRATEGY.md).

# --- Kaggle access -----------------------------------------------------------
kaggle>=2.2.4           # CLI 2.x: auth via `kaggle auth login`, KAGGLE_API_TOKEN,
                        # ~/.kaggle/access_token, or legacy ~/.kaggle/kaggle.json

# --- Tabular / analysis ------------------------------------------------------
pandas>=2.2
numpy>=1.26
pyarrow>=16.0           # parquet for the DICOM header scan output
scikit-learn>=1.5       # AUC, calibration, grouped CV splitters
scipy>=1.13

# --- DICOM (headers only locally; pixels only inside Kaggle kernels) ----------
pydicom>=3.0

# --- Report text (Phase 1) ---------------------------------------------------
regex>=2024.5.15        # Unicode-aware \p{...} classes, needed for ~12 languages
unidecode>=1.3

# --- Plotting for EDA --------------------------------------------------------
matplotlib>=3.9

# --- Dev ---------------------------------------------------------------------
ruff>=0.6
pytest>=8.0
